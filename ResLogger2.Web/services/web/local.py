from project.index_repository import IndexRepository

def test():

    index = IndexRepository("C:\\Users\\Liam\\Documents\\repos\\ResLogger2\\ResLogger2.Web\\services\\web\\index")

    # test_file = "bg/ex2/05_zon_z3/ome/z3o2/texture/z3o2_a1_flo04_n.tex"
    # print(IndexRepository.get_category_id(test_file))
    #
    # if not index.exists(test_file):
    #     print(f"failed: {test_file}")
    # test_file = "chara/demihuman/d0001/obj/equipment/e0015/model/d0001e0015_met.mdl"
    # if not index.exists(test_file):
    #     print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/02/0005/texture/dor_t4_m0005_0a_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "sound/battle/wep/armourer_02.scd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgparts/w1h1_w1_grdl.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/06/0002/texture/orf_t4_m0002_0a_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/accessory/a0001/model/c1101a0001_ear.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/05/0009/texture/osg_t4_m0009_0a_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/grass/005_006_005_m.ggd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/weapon/w2015/animation/s0001/body/material.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c1101/obj/face/f0102/texture/--c1101f0102_fac_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/equipment/e0007/material/v0009/mt_c0101e0007_glv_a.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/omen/texture/squa_s001bf.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c0101/animation/a0001/bt_2gn_emp/ws/bt_2gn_emp/ws_s07.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/action/mgc_2rp_r02/texture/uv_ora1.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0239/bgparts/gar_b0_m0239b.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/weapon/w5241/obj/body/b0001/texture/v01_w5241b0001_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/grass/007_006_003_h.ggd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgparts/w1h1_p0_plt2.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/00/0005/texture/wlc_t4_m0005_0b_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/demihuman/d0001/obj/equipment/e0015/material/v0001/mt_d0001e0015_sho_a.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/housingexterior_0.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/action/normal/swim_under_searchaction_start.tmb"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/00/0010/texture/wlc_t4_m0010_0a_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/common/general/0000/texture/com_b0_m0000_0a_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/equipment/e0010/texture/v04_c0101e0010_sho_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/common/vfx_hou_out1/texture/watr101_y.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/common/texture/uv_ora3s.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/material/w1h1_w1_taf01a.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/house/texture/bil_com_m0000_4t_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/est_e1/hou/dyna/c_fnc/0001/collision/e1h0_f_fnc0001c.pcb"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgparts/w1h1_w1_wall0.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c0101/animation/a0001/bt_common/ability/2gn_machin/abl025_start.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/est_e1/hou/dyna/c_fnc/0003/texture/e1h0_f_fnc0003_0b_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgplate/0209.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/common/vfx_hou_out1/texture/smok019_y.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/world/hal/001/bgparts/w_hal_001_08b.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c0201/obj/face/f0005/texture/--c0201f0005_fac_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0134/bgparts/gar_b0_m0134.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/weapon/w1911/animation/a0001/wp_common/resident/weapon.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/action/mgc_whm_mgc004/texture/kona10ah.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c1801/animation/a0001/bt_common/emote/pose03_start.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/dyna/opt/rf/0005/texture/opt_rf_m0005_0a_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgparts/w1h1_a0_ligh3.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c0101/animation/a0001/bt_common/normal/u_fueaction_start.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgplate/0101.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/ws/chk_ws_s01/texture/tk_snic002c.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/material/w1h1_t1_rivec.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/texture/w1h1_b0_mtst1_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/weapon/w5301/obj/body/b0003/model/w5301b0003.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/item_19500_en.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/item_35000_en.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "ui/uld/image3.uld"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgplate/0040.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/common/vfx_hou_out1/texture/kira_01_y.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0176/texture/gar_b0_m0176_0a_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c0101/animation/a0001/bt_arm_emp/craft/end_sub.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/common/vfx_hou_dyna1/texture/bt_hino_i.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c1101/obj/face/f0102/texture/--c1101f0102_etc_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/04/0007/texture/fnc_t4_m0007_0a_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0176/texture/gar_b0_m0176_0c_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/fst_f1/hou/dyna/common/texture/f1h0_com_wal0003_1b_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/action/mgc_swl_mgc011/texture/mokumoku01ah.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/common/vfx_hou_out1/eff/ogene_0064_y.avfx"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0123/material/gar_b0_m0123_0d.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/questeffectdefine_65536.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "ui/icon/000000/000844_hr1.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c1101/obj/face/f0001/texture/--c1101f0001_etc_b_m.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/demihuman/d0001/obj/equipment/e0001/texture/v01_d0001e0001_dwn_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/sea_s1/hou/dyna/common/texture/s1h0_com_wal0003_0b_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgplate/0215.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/bgb/2gb_ws_s01/texture/ksk_by02_i.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/texture/dummy_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/human/c0101/animation/a0001/bt_common/normal/fueaction_start.pap"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/00/0014/bgparts/wll_t4_m0014.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/sound/wil/wil_spot_mizutamari_mono.scd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/01/0009/texture/rfc_t4_m0009_0e_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0012/texture/gar_b0_m0012_0b_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/04/0010/bgparts/fnc_t4_m0010a.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/scenariotree_65564.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0223/texture/gar_b0_m0223_0a_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "vfx/pc_common/pc_rol041/texture/ligt056_c.atex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/custom/005/cmndefretainerbell_00544_0_en.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/dyna/s_rof/0001/asset/w1h0_s_rof0001.sgb"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "exd/item_33500_en.exd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0224/texture/gar_b0_m0224_0d_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/02/0016/bgparts/dor_t4_m0016.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/00/0016/bgparts/wlc_t4_m0016h.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0209/material/gar_b0_m0209_0d.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/equipment/e0234/e0234.imc"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "chara/weapon/w1911/obj/body/b0001/b0001.imc"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/grass/005_006_006_l.ggd"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/bgplate/0097.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/outdoor/general/0021/texture/gar_b0_m0021_0a_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/dyna/opt/sg/0103/bgparts/opt_sg_m0103.mdl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/house/texture/bil_com_m0000_5n_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/04/0002/texture/fnc_t4_m0002_0a_d.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/dyna/c_fnc/0000/texture/w1h0_f_fnc0000_0a_s.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bg/ffxiv/wil_w1/hou/w1h1/texture/w1h1_b0_roof1_n.tex"
    if not index.exists(test_file):
        print(f"failed: {test_file}")
    test_file = "bgcommon/hou/craft/bil/06/0005/material/orf_t4_m0005_0a.mtrl"
    if not index.exists(test_file):
        print(f"failed: {test_file}")


if __name__ == "__main__":
    test()